apiVersion: ai.sap.com/v1alpha1
kind: ServingTemplate
metadata:
  name: vendor-reco-serve
  annotations:
    ext.ai.sap.com/islm_executable_type: "inference"
    ext.ai.sap.com/islm_inference_type: "online"
    ext.ai.sap.com/islm_released_version: "true"
    scenarios.ai.sap.com/name: "vendor-recommendation"
    scenarios.ai.sap.com/description: "Serve the vendor recommendation model as a REST API"
spec:
  image: docker.io/bhuni/vendor-final-app:latest
  ports:
    - containerPort: 8080
  resources:
    limits:
      memory: 2Gi
      cpu: 1
